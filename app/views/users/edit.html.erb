<main>
    <div class="container">
        <div class="row pt-5">
            <div class="mx-auto">
                <h1>User info</h1>
                <%= flash[:notice] %>
                <% if @user.errors.any? %>
                    <%= @user.errors.count %>error prohibited this obj from being <br>saved:
                    <ul>
                        <% @user.errors.full_messages.each do |message| %>
                        <li><%= message %></li>
                        <% end %>
                    </ul>
                <% end %>
                
                <%= form_with model: @user do |f| %>
                <div class="mb-3">
                    <label for="exampleFormControlInput1" class="form-label">Name</label>
                    <%= f.text_field :name, autofocus: true, class: "form-control"%>
                </div>
            </div>
        </div>
                    
                
                      
                    <label for="inputImage">Image</label>
                    <%= f.file_field :profile_image, placeholder: "Image", accept: "image/*" %>
                            
                    <div class="row">
                        <div class="col-sm-6">
                            <div class="mb-3">
                                <label for="exampleFormControlTextarea1" class="col-form-label">Introduction</label>
                                <%= f.text_area :introduction, autofocus: true, class: "form-control", id:"exampleFormControlTextarea1", rows: 3 %>
                            </div>
                        </div>
                    </div>
                    <%= f.submit "Update User", class: "btn btn-secondary" %>
                <% end %>
                
            </div>
        </div>
    </div>
</main>
<footer class="text-center mt-5">CopyRight Infratop.inc</footer>